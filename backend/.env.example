FIRECRAWL_API_KEY="your_firecrawl_api_key_here"
OPENAI_API_KEY="your_openai_api_key_here"
# Note: Memento API key is not strictly required depending on the mem0 configuration,
# but can be added here if using a managed platform
