-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.funding_intelligence (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  validation_id uuid NOT NULL,
  competitor_name text NOT NULL,
  funding_rounds jsonb,
  total_funding text,
  last_round_date text,
  source_url text,
  scraped_at timestamp with time zone DEFAULT now(),
  CONSTRAINT funding_intelligence_pkey PRIMARY KEY (id),
  CONSTRAINT funding_intelligence_validation_id_fkey FOREIGN KEY (validation_id) REFERENCES public.validations(id)
);
CREATE TABLE public.jobs_intelligence (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  validation_id uuid NOT NULL,
  competitor_name text NOT NULL,
  open_roles jsonb,
  scraped_at timestamp with time zone DEFAULT now(),
  CONSTRAINT jobs_intelligence_pkey PRIMARY KEY (id),
  CONSTRAINT jobs_intelligence_validation_id_fkey FOREIGN KEY (validation_id) REFERENCES public.validations(id)
);
CREATE TABLE public.patent_intelligence (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  validation_id uuid NOT NULL,
  competitor_name text NOT NULL,
  patents_found jsonb,
  scraped_at timestamp with time zone DEFAULT now(),
  CONSTRAINT patent_intelligence_pkey PRIMARY KEY (id),
  CONSTRAINT patent_intelligence_validation_id_fkey FOREIGN KEY (validation_id) REFERENCES public.validations(id)
);
CREATE TABLE public.pricing_intelligence (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  validation_id uuid NOT NULL,
  competitor_name text NOT NULL,
  competitor_url text,
  pricing_tiers jsonb NOT NULL,
  gap_analysis text,
  scraped_at timestamp with time zone DEFAULT now(),
  CONSTRAINT pricing_intelligence_pkey PRIMARY KEY (id),
  CONSTRAINT pricing_intelligence_validation_id_fkey FOREIGN KEY (validation_id) REFERENCES public.validations(id)
);
CREATE TABLE public.rag_chunks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  validation_id uuid NOT NULL,
  source_url text,
  chunk_index integer NOT NULL,
  chunk_text text NOT NULL,
  embedding USER-DEFINED,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT rag_chunks_pkey PRIMARY KEY (id),
  CONSTRAINT rag_chunks_validation_id_fkey FOREIGN KEY (validation_id) REFERENCES public.validations(id)
);
CREATE TABLE public.report_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  validation_id uuid NOT NULL,
  version_number integer NOT NULL DEFAULT 1,
  report_json jsonb NOT NULL,
  markdown_report text,
  diff_summary jsonb,
  change_narrative text,
  tokens_used integer DEFAULT 0,
  estimated_cost double precision DEFAULT 0.0,
  model_version text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT report_versions_pkey PRIMARY KEY (id),
  CONSTRAINT report_versions_validation_id_fkey FOREIGN KEY (validation_id) REFERENCES public.validations(id)
);
CREATE TABLE public.social_sentiment (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  validation_id uuid NOT NULL,
  platform text NOT NULL DEFAULT 'reddit'::text,
  competitor_name text,
  positive_count integer DEFAULT 0,
  negative_count integer DEFAULT 0,
  neutral_count integer DEFAULT 0,
  market_buzz_score double precision DEFAULT 0.0,
  sample_posts jsonb,
  analyzed_at timestamp with time zone DEFAULT now(),
  CONSTRAINT social_sentiment_pkey PRIMARY KEY (id),
  CONSTRAINT social_sentiment_validation_id_fkey FOREIGN KEY (validation_id) REFERENCES public.validations(id)
);
CREATE TABLE public.traffic_intelligence (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  validation_id uuid NOT NULL,
  competitor_name text NOT NULL,
  traffic_data jsonb,
  scraped_at timestamp with time zone DEFAULT now(),
  CONSTRAINT traffic_intelligence_pkey PRIMARY KEY (id),
  CONSTRAINT traffic_intelligence_validation_id_fkey FOREIGN KEY (validation_id) REFERENCES public.validations(id)
);
CREATE TABLE public.user_settings (
  user_id uuid NOT NULL,
  webhook_url text,
  webhook_secret text,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_settings_pkey PRIMARY KEY (user_id),
  CONSTRAINT user_settings_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.validations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  idea_description text NOT NULL,
  target_market text,
  budget_constraints text,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::validation_status,
  idempotency_key text NOT NULL,
  idea_hash text NOT NULL,
  processing_started_at timestamp with time zone,
  retry_count integer DEFAULT 0 CHECK (retry_count >= 0 AND retry_count <= 5),
  last_retry_at timestamp with time zone,
  model_version character varying,
  tokens_used integer DEFAULT 0 CHECK (tokens_used >= 0),
  estimated_cost numeric DEFAULT 0.000000 CHECK (estimated_cost >= 0::numeric),
  report_json jsonb,
  markdown_report text,
  error_message text,
  idea_embedding USER-DEFINED,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  gemini_report jsonb,
  groq_report jsonb,
  consensus_confidence double precision,
  model_agreement jsonb,
  pricing_data jsonb,
  funding_data jsonb,
  sentiment_data jsonb,
  patent_data jsonb,
  jobs_data jsonb,
  traffic_data jsonb,
  CONSTRAINT validations_pkey PRIMARY KEY (id),
  CONSTRAINT validations_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.workspace_members (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  workspace_id uuid NOT NULL,
  user_id uuid NOT NULL,
  email text NOT NULL,
  role text NOT NULL DEFAULT 'viewer'::text,
  invited_at timestamp with time zone DEFAULT now(),
  CONSTRAINT workspace_members_pkey PRIMARY KEY (id),
  CONSTRAINT workspace_members_workspace_id_fkey FOREIGN KEY (workspace_id) REFERENCES public.workspaces(id),
  CONSTRAINT workspace_members_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.workspaces (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  created_by uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT workspaces_pkey PRIMARY KEY (id),
  CONSTRAINT workspaces_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);